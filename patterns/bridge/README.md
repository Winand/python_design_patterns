# Bridge pattern
Bridge pattern uses composition to inject different 
implementations into an abstract class, which also
can be extended through inheritance. So the two sides
of the "bridge" can be developed independently.

For comparison, in [Strategy Pattern](../strategy/README.md),
an algorithm is injected into a concrete implementation.
