# Retry Pattern
Retry pattern allows you to retry a function call if it fails.
There are preferences like the number of attempts, delay, etc.

## See also
- [tenacity](https://github.com/jd/tenacity)
