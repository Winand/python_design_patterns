# Template Pattern
Template pattern has a base class that defines the skeleton of an algorithm,
then inheritance is used to modify steps without changing the overall structure.

For comparison, see the [Strategy Pattern](../strategy/README.md).
It uses composition and dependency injection to modify behavior of
the base class at runtime.
